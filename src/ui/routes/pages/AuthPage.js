import { Fragment } from 'react';
import Login from '../../components/Login/Login';

const Auth = () => {
   return (
      <Fragment>
         <Login />
      </Fragment>
   );
};
export default Auth;
